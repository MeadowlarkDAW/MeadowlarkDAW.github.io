export interface Feature {
    title: string,
    text: string,
    svg: string,
}

export interface Milestone {
    name: string,
    description: string,
    expected: string,
    color: string,
    icon: string,
}